# Variables for the db group (instaclone-db-0)
# postgres credentials are loaded from secrets.yml (generated by generate_secrets.yml)
postgres_db:   instaclone
postgres_user: instaclone

app_dir: /root/app
tz: Europe/Berlin
